interface:
  display_name: "Artifact to Skill Extractor"
  short_description: "Compile evidence into executable production systems"
  default_prompt: "Use $artifact-to-skill-extractor to inspect my evidence, compile a scoped production system, generate a new concrete output, and evaluate it."
